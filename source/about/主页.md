---
author: couriourc
dg-home: true
dg-publish: true
title: 主页
date-created: 2023-07-01
date-modified: 2023-07-02
---

```dataview
TABLE dg-publish AS "发布状态",dateformat(date-created,"DD") AS "发布时间" 
FROM "_posts"
WHERE dg-publish=true
SORT  DESC
```

```dataviewjs
const pages =  dv.pages()
           .groupBy(p => p.file.folder)
           .sort(p=> p.rows.length)
           .map(p => ({name: p.key || "根目录", value: p.rows.length}))
           .array().reverse();
const colorList = ['#f36c6c', '#e6cf4e', '#20d180', '#0093ff'];
const datas = pages

const option = {
	title: {
          text: '文件夹文件数量矩阵图'
        },
    series: {
        type: 'treemap',
        itemStyle: {
            color: 'rgba(109, 40, 40, 1)',
            borderWidth: 2,
            borderColor: 'rgba(255, 255, 0, 0)',
        },
        data: pages
        
    },
    type: 'basicTreemap',
};

app.plugins.plugins['obsidian-echarts'].render(option, this.container)

```
